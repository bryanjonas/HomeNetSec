# HomeNetSec dashboard binds (example)
#
# Create assets/.env (uncommitted) with the real IPs for THIS host.
#
# Bind ONLY to LAN + Tailscale addresses. Do NOT use 0.0.0.0.
# Use the LAN address of the host running the dashboard.
HOMENETSEC_LAN_BIND_IP=LAN_HOST_IP

# Use the Tailscale address of the host running the dashboard.
HOMENETSEC_TS_BIND_IP=TS_HOST_IP

HOMENETSEC_DASHBOARD_PORT=8088

# Optional: base URL for linking to the dashboard (used by the Telegram wrapper if enabled).
# Examples:
#   HOMENETSEC_DASHBOARD_BASE_URL=http://TS_HOST_IP:8088
#   HOMENETSEC_DASHBOARD_BASE_URL=http://myhost:8088   (MagicDNS)
#HOMENETSEC_DASHBOARD_BASE_URL=http://TS_HOST_IP:8088
