# HomeNetSec environment configuration
# Copy to .env and customize for your environment.
#
# This file is sourced by scripts and read by docker-compose.

# Output directory for PCAPs, logs, reports, and state
# Default (if unset): ./output
HOMENETSEC_WORKDIR=/path/to/your/data/HomeNetSec

# Dashboard binds — use LAN + Tailscale addresses only (no 0.0.0.0)
# Use the LAN address of the host running the dashboard.
HOMENETSEC_LAN_BIND_IP=LAN_HOST_IP

# Use the Tailscale address of the host running the dashboard.
HOMENETSEC_TS_BIND_IP=TS_HOST_IP

HOMENETSEC_DASHBOARD_PORT=8088

# Optional: base URL for linking to the dashboard (used by scripts if enabled).
# Examples:
#   HOMENETSEC_DASHBOARD_BASE_URL=http://TS_HOST_IP:8088
#   HOMENETSEC_DASHBOARD_BASE_URL=http://myhost:8088   (MagicDNS)
#HOMENETSEC_DASHBOARD_BASE_URL=http://TS_HOST_IP:8088
